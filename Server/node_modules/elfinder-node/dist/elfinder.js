"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.driverRegistry = exports.LFS = exports.LocalFileStorage = void 0;
exports.elfinder = elfinder;
const express_1 = __importDefault(require("express"));
const busboy = __importStar(require("express-busboy"));
const lfs_1 = __importDefault(require("./lfs"));
exports.LocalFileStorage = lfs_1.default;
exports.LFS = lfs_1.default;
const utils_1 = require("./utils");
const driverRegistry_1 = require("./driverRegistry");
Object.defineProperty(exports, "driverRegistry", { enumerable: true, get: function () { return driverRegistry_1.driverRegistry; } });
const router = express_1.default.Router();
function elfinder(roots) {
    // Initialize the driver registry with all volumes
    driverRegistry_1.driverRegistry.initialize(roots);
    busboy.extend(router, {
        upload: true,
    });
    router.get('/', async (req, res) => {
        const cmd = req.query.cmd;
        try {
            const driver = driverRegistry_1.driverRegistry.getDriverForRequest(req.query);
            if (typeof driver[cmd] !== 'function')
                throw (0, utils_1.notImplementedError)(cmd);
            const result = await driver[cmd](req.query, res);
            if (result)
                res.json(result);
        }
        catch (e) {
            console.error(req.query, e);
            res.status(500).json({ error: e.message });
        }
    });
    router.post('/', async (req, res) => {
        const cmd = req.body.cmd;
        try {
            const driver = driverRegistry_1.driverRegistry.getDriverForRequest(req.query);
            if (typeof driver[cmd] !== 'function')
                throw (0, utils_1.notImplementedError)(cmd);
            const result = await driver[cmd](req.body, res, req.files?.['upload[]']);
            if (result)
                res.json(result);
        }
        catch (e) {
            console.error(req.body, e);
            res.status(500).json({ error: e.message });
        }
    });
    return router;
}
//# sourceMappingURL=elfinder.js.map