import { Config, DecodedPath, ParsedPath, FileItem, FileInfo, CopyMoveOptions, CopyMoveResult } from './types';
export type LFSConfig = Config & {
    init?: Function;
    path: string;
    tmbroot: string;
};
export type LFSConfigInput = Partial<Config> & {
    path: string;
    tmbroot?: string;
};
export declare const compress: (files: string[], dest: string, config: LFSConfig) => Promise<boolean>;
export declare const copy: (opts: CopyMoveOptions, config: LFSConfig) => Promise<CopyMoveResult>;
export declare const decode: (dir: string, config: LFSConfig) => DecodedPath;
export declare const encode: (dir: string, config: LFSConfig) => string;
export declare const extract: (source: string, dest: string) => Promise<string[]>;
export declare const info: (p: string, config: LFSConfig) => Promise<FileInfo>;
export declare const init: (config: LFSConfig) => Promise<FileInfo[]>;
export declare const move: (opts: CopyMoveOptions, config: LFSConfig) => Promise<CopyMoveResult>;
export declare const parse: (p: string, config: LFSConfig) => ParsedPath;
export declare const readdir: (dir: string) => Promise<FileItem[]>;
export declare const suffix: (name: string, suff: string) => string;
export declare const volume: (p: string, config: LFSConfig) => number;
export interface ChunkUploadOptions {
    chunkName: string;
    chunkFile: string;
    range: string;
    destinationDir: string;
}
export interface ChunkUploadResult {
    isComplete: boolean;
    finalPath?: string;
    chunkPath: string;
}
/**
 * Handles uploading a single chunk of a file
 * Returns information about whether the upload is complete and where the file is located
 */
export declare const handleChunkUpload: (opts: ChunkUploadOptions) => Promise<ChunkUploadResult>;
//# sourceMappingURL=lfs.utils.d.ts.map